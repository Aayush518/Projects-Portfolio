---
---
<section class="min-h-screen relative flex items-center py-12 sm:py-20 overflow-hidden">
  {/* Background Elements */}
  <div class="absolute inset-0 bg-dark-100"></div>
  <div class="absolute inset-0 bg-grid opacity-[0.02]"></div>
  <div class="absolute top-1/2 -translate-y-1/2 left-1/2 -translate-x-1/2 w-[300px] sm:w-[500px] h-[300px] sm:h-[500px] 
              bg-gradient-radial from-primary/20 via-primary/5 to-transparent blur-3xl"></div>
  <div class="absolute bottom-0 left-0 w-48 sm:w-96 h-48 sm:h-96 bg-accent/5 rounded-full blur-3xl"></div>

  <div class="container mx-auto px-4 relative">
    <div class="max-w-6xl mx-auto">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center">
        {/* Left Column - Content */}
        <div class="space-y-6 sm:space-y-8 text-center lg:text-left">
          {/* Badge */}
          <div class="inline-flex items-center gap-2 px-3 sm:px-4 py-1.5 sm:py-2 rounded-full 
                      bg-accent/10 border border-accent/20 backdrop-blur-sm">
            <span class="w-1.5 sm:w-2 h-1.5 sm:h-2 rounded-full bg-accent animate-pulse"></span>
            <span class="text-accent/90 text-xs sm:text-sm">Available for Projects</span>
          </div>

          {/* Main Content */}
          <div class="space-y-4">
            <h1 class="text-4xl sm:text-5xl md:text-6xl font-bold leading-tight sm:leading-tight md:leading-tight">
              Full Stack Developer <br />& Project Lead
            </h1>
            <p class="text-base sm:text-lg text-white/70 max-w-xl mx-auto lg:mx-0">
              Building scalable solutions with modern technologies. Focused on creating impactful digital experiences.
            </p>
          </div>

          {/* CTA Buttons */}
          <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
            <a
              href="#projects"
              class="group relative px-8 py-4 bg-gradient-to-r from-[#9b111e] to-[#ff1616] text-white rounded-lg
                     overflow-hidden shadow-xl shadow-[#9b111e]/20 hover:shadow-[#ff1616]/20
                     transition-all duration-300 hover:-translate-y-1 active:translate-y-0"
            >
              <span class="relative z-10 flex items-center gap-2 font-medium">
                View My Work
                <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" 
                     fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                        d="M13 7l5 5m0 0l-5 5m5-5H6" />
                </svg>
              </span>
              <div class="absolute inset-0 bg-gradient-to-r from-[#ff1616] to-[#9b111e] opacity-0 
                          group-hover:opacity-100 transition-opacity duration-300" />
            </a>
            <a
              href="#contact"
              class="group relative px-8 py-4 bg-dark-200/50 text-white rounded-lg
                     overflow-hidden border-2 border-[#ff1616] shadow-xl hover:shadow-[#ff1616]/20
                     transition-all duration-300 hover:-translate-y-1 active:translate-y-0"
              onclick="document.getElementById('contact').scrollIntoView({ behavior: 'smooth' })"
            >
              <span class="relative z-10 flex items-center gap-2 font-medium">
                Get in Touch
                <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" 
                     fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                        d="M20 12h-9m3 3l-3-3 3-3" />
                </svg>
              </span>
              <div class="absolute inset-0 bg-gradient-to-r from-[#9b111e] to-[#ff1616] opacity-0 
                          group-hover:opacity-100 transition-opacity duration-300" />
            </a>
          </div>

          {/* Tech Stack */}
          <div class="pt-8 sm:pt-12">
            <h3 class="text-white/50 font-mono text-xs sm:text-sm uppercase tracking-wider 
                      flex items-center gap-2 mb-4 sm:mb-6 justify-center lg:justify-start">
              <span class="w-6 sm:w-8 h-[1px] bg-primary/40"></span>
              Tech Expertise
            </h3>
            <div class="flex flex-wrap gap-2 sm:gap-3 justify-center lg:justify-start">
              {[
                'Full Stack Development', 'NLP Research', 'Project Management',
                'Problem Solving', 'System Architecture', 'Cloud Solutions'
              ].map((skill) => (
                <div class="px-3 sm:px-4 py-1.5 sm:py-2 bg-dark-200/50 backdrop-blur-sm rounded-full 
                            border border-primary/20 hover:border-primary/40 transition-all 
                            duration-300 group">
                  <span class="text-white/70 text-xs sm:text-sm group-hover:text-white/90 
                             transition-colors">{skill}</span>
                </div>
              ))}
            </div>
          </div>
        </div>

        {/* Right Column - Code Editor */}
        <div class="relative w-full max-w-2xl mx-auto lg:max-w-none">
          <div class="relative backdrop-blur-xl rounded-xl overflow-hidden 
                    border border-accent/10 shadow-2xl shadow-accent/5">
            {/* Editor Header */}
            <div class="bg-dark-200/90 p-3 sm:p-4 border-b border-accent/10">
              <div class="flex items-center">
                <div class="flex gap-1.5 sm:gap-2">
                  <div class="w-2.5 sm:w-3 h-2.5 sm:h-3 rounded-full bg-red-500/30"></div>
                  <div class="w-2.5 sm:w-3 h-2.5 sm:h-3 rounded-full bg-yellow-500/30"></div>
                  <div class="w-2.5 sm:w-3 h-2.5 sm:h-3 rounded-full bg-green-500/30"></div>
                </div>
                <div class="flex-1 flex justify-center">
                  <span class="text-white/50 text-xs sm:text-sm font-mono px-2 sm:px-3 py-0.5 sm:py-1 
                             rounded-md bg-dark-300/50">
                    philosophy.ts
                  </span>
                </div>
              </div>
            </div>

            {/* Editor Content */}
            <div class="p-4 sm:p-6 lg:p-8 space-y-4 sm:space-y-6 lg:space-y-8 bg-dark-200/50">
              {/* Against Section */}
              <div class="space-y-4">
                <div class="flex items-center gap-3">
                  <span class="text-purple-400/90 font-mono">// What I am againt is The "Hustle Culture" Trap</span>
                  <span class="px-2 py-0.5 text-xs font-mono bg-red-500/10 text-red-400/90 rounded-full">
                    @deprecated
                  </span>
                </div>
                <div class="pl-6 space-y-2">
                  <div class="relative">
                    <div class="absolute -left-4 top-0 bottom-0 w-[2px] bg-red-400/20"></div>
                    <div class="space-y-2 text-red-400/70 font-mono leading-relaxed line-through opacity-70">
                      <p>Sleep for 28 dayss,</p>
                      <p>grind for 48 hours,</p>
                      <p>cry about burnout.</p>
                      <p>Repeat.</p>
                    </div>
                  </div>
                </div>
              </div>

              {/* Beliefs Section */}
              <div class="space-y-4">
                <div class="flex items-center gap-3">
                  <span class="text-purple-400/90 font-mono">// What I believe in is Sustainable Development Philosophy</span>
                  <span class="px-2 py-0.5 text-xs font-mono bg-green-500/10 text-green-400/90 rounded-full">
                    @current
                  </span>
                </div>
                <div class="pl-6 relative">
                  <div class="absolute -left-4 top-0 bottom-0 w-[2px] bg-green-400/20"></div>
                  <div class="space-y-2 text-green-400/90 font-mono leading-relaxed">
                    <p>Consistent daily progress</p>
                    <p>15 minutes daily > 48 hours of burnout</p>
                    <p>Sustainable development</p>
                    <p>Work-life harmony</p>
                  </div>
                </div>
              </div>

              {/* Philosophy Box */}
              <div class="mt-8">
                <div class="p-4 bg-dark-300/50 rounded-lg border border-primary/10 
                            backdrop-blur-sm hover:border-primary/20 transition-colors">
                  <div class="space-y-3">
                    <p class="text-white/70 font-mono text-sm leading-relaxed">
                      <span class="text-primary/80">const</span> philosophy <span class="text-white/50">=</span> {
                      <span class="text-green-400 block pl-4">
                        "Small steps daily > Giant leaps at deadline"
                      </span>
                      };
                    </p>
                    <div class="pt-2 border-t border-primary/5">
                      <p class="text-white/60 font-mono text-sm italic flex items-center gap-2">
                        <span class="w-2 h-2 rounded-full bg-green-500/50 animate-pulse"></span>
                        Want to discuss sustainable development practices? Let's grab coffee ☕
                      </p>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>

          {/* Floating Elements */}
          <div class="absolute -top-8 -right-8 w-32 sm:w-40 h-32 sm:h-40 bg-primary/10 rounded-full blur-3xl"></div>
          <div class="absolute -bottom-8 -left-8 w-32 sm:w-40 h-32 sm:h-40 bg-accent/10 rounded-full blur-3xl"></div>
        </div>
      </div>
    </div>
  </div>

  {/* Scroll Indicator */}
  <div class="absolute bottom-8 sm:bottom-12 left-1/2 -translate-x-1/2 flex flex-col items-center 
              gap-2 opacity-80 hover:opacity-100 transition-opacity cursor-pointer"
       onclick="document.getElementById('about').scrollIntoView({ behavior: 'smooth' })">
    <span class="text-white/50 text-xs sm:text-sm font-mono tracking-wide">Scroll to Explore</span>
    <div class="w-5 sm:w-6 h-8 sm:h-10 border-2 border-white/20 rounded-full p-1">
      <div class="w-1 sm:w-1.5 h-1 sm:h-1.5 bg-white/50 rounded-full mx-auto animate-scroll-down"></div>
    </div>
  </div>
</section>

<style>
  .bg-grid-pattern {
    background-image: linear-gradient(rgba(255, 255, 255, 0.1) 1px, transparent 1px),
                      linear-gradient(90deg, rgba(255, 255, 255, 0.1) 1px, transparent 1px);
    background-size: 20px 20px;
  }

  @keyframes scroll-down {
    0% {
      transform: translateY(0);
      opacity: 1;
    }
    75% {
      transform: translateY(20px);
      opacity: 0;
    }
    76%, 100% {
      transform: translateY(0);
      opacity: 0;
    }
  }

  .animate-scroll-down {
    animation: scroll-down 2s cubic-bezier(0.65, 0, 0.35, 1) infinite;
  }
</style>