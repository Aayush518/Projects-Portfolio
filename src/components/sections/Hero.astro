---
---
<section class="min-h-screen relative overflow-hidden bg-dark" data-scroll-section>
  <!-- Background Effects -->
  <div class="absolute inset-0">
    <div class="absolute inset-0 bg-gradient-radial from-primary-light/10 via-transparent to-transparent"></div>
    <div class="absolute inset-0 bg-grid opacity-[0.05] bg-[size:50px_50px]"></div>
  </div>

  <div class="container mx-auto px-4 pt-32 relative z-10">
    <div class="max-w-6xl mx-auto">
      <div class="flex flex-col items-start space-y-8">
        <!-- Title Badge -->
        <div class="inline-flex items-center px-4 py-2 bg-dark-200/50 backdrop-blur-sm rounded-full border border-primary/20">
          <span class="text-primary-light font-mono text-sm">Problem Solver & Innovation Driver</span>
        </div>
        
        <!-- Main Heading -->
        <div class="space-y-4">
          <h1 class="text-4xl md:text-7xl font-display font-bold tracking-tight">
            <span class="text-white">Hi, I'm</span>
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary via-accent to-highlight"> Aayush</span>
          </h1>
          <p class="text-2xl md:text-4xl font-display text-white/80">
            Turning Complex Challenges into
            <span class="text-accent">Elegant Solutions</span>
          </p>
        </div>

        <!-- Description -->
        <p class="text-lg md:text-xl text-white/70 max-w-3xl leading-relaxed">
          A disciplined problem solver with expertise in Full Stack Development, NLP Research, 
          and Project Management. Focused on delivering impactful results through innovative 
          solutions and strategic thinking.
        </p>

        <!-- CTA Buttons -->
        <div class="flex flex-wrap gap-6">
          <a href="#projects" 
             class="group px-8 py-4 bg-primary hover:bg-primary-dark rounded-lg transition-all duration-300 relative overflow-hidden">
            <span class="relative z-10 text-white font-medium">View My Work</span>
            <div class="absolute inset-0 bg-white/20 translate-y-[200%] group-hover:translate-y-0 transition-transform duration-500"></div>
          </a>
          <a href="#contact" 
             class="group px-8 py-4 border-2 border-primary text-primary hover:text-white rounded-lg transition-all duration-300 relative overflow-hidden">
            <span class="relative z-10">Let's Connect</span>
            <div class="absolute inset-0 bg-primary translate-y-[200%] group-hover:translate-y-0 transition-transform duration-500"></div>
          </a>
        </div>

        <!-- Tech Stack -->
        <div class="pt-12 w-full">
          <h3 class="text-white/50 font-mono text-sm mb-6">TECH EXPERTISE</h3>
          <div class="flex flex-wrap gap-4">
            {[
              'Full Stack Development', 'NLP Research', 'Project Management',
              'Problem Solving', 'Team Leadership', 'Innovation Strategy'
            ].map((skill) => (
              <div class="px-4 py-2 bg-dark-200/50 backdrop-blur-sm rounded-full border border-primary/20 
                          hover:border-primary/40 hover:bg-dark-200/70 transition-all duration-300">
                <span class="text-white/70 text-sm">{skill}</span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .bg-grid-pattern {
    background-image: linear-gradient(rgba(255, 255, 255, 0.1) 1px, transparent 1px),
                      linear-gradient(90deg, rgba(255, 255, 255, 0.1) 1px, transparent 1px);
    background-size: 20px 20px;
  }

  @keyframes waveform {
    0%, 100% {
      transform: scaleY(0.5);
    }
    50% {
      transform: scaleY(1);
    }
  }
</style>