---
---
<section class="min-h-screen relative flex items-center py-20 overflow-hidden">
  {/* Background Elements */}
  <div class="absolute inset-0 bg-dark-100"></div>
  <div class="absolute inset-0 bg-grid opacity-[0.02]"></div>
  <div class="absolute top-1/2 -translate-y-1/2 left-1/2 -translate-x-1/2 w-[500px] h-[500px] 
              bg-gradient-radial from-primary/20 via-primary/5 to-transparent blur-3xl"></div>
  <div class="absolute bottom-0 left-0 w-96 h-96 bg-accent/5 rounded-full blur-3xl"></div>

  <div class="container mx-auto px-4 relative">
    <div class="max-w-6xl mx-auto">
      <div class="grid lg:grid-cols-2 gap-12 items-center">
        {/* Left Column - Content */}
        <div class="space-y-8">
          {/* Badge */}
          <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary/10 
                      border border-primary/20 backdrop-blur-sm">
            <span class="w-2 h-2 rounded-full bg-primary animate-pulse"></span>
            <span class="text-primary/90 text-sm">Available for Projects</span>
          </div>

          {/* Main Heading */}
          <div class="space-y-4">
            <h1 class="text-5xl md:text-6xl lg:text-7xl font-display font-bold">
              <span class="text-white">Hi, I'm</span>
              <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary to-accent">
                Aayush
              </span>
            </h1>
            <h2 class="text-3xl md:text-4xl font-display text-white/80">
              Problem Solver & Full Stack Developer
            </h2>
          </div>

          {/* Description */}
          <p class="text-xl text-white/70 leading-relaxed max-w-2xl">
            Transforming complex challenges into elegant solutions through code and creativity. 
            Specializing in full-stack development, NLP research, and disciplined project execution.
          </p>

          {/* CTA Buttons */}
          <div class="flex flex-wrap gap-4 pt-4">
            <a
              href="#projects"
              class="inline-flex items-center gap-2 px-8 py-4 bg-gradient-to-r from-primary to-accent 
                     text-white rounded-lg transition-all duration-300 hover:-translate-y-1 
                     hover:shadow-lg hover:shadow-primary/20 group"
              onclick="document.getElementById('projects').scrollIntoView({ behavior: 'smooth' })"
            >
              View My Work
              <svg 
                class="w-5 h-5 group-hover:translate-x-1 transition-transform" 
                fill="none" 
                viewBox="0 0 24 24" 
                stroke="currentColor"
              >
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                      d="M14 5l7 7m0 0l-7 7m7-7H3" />
              </svg>
            </a>
            <a
              href="#contact"
              class="inline-flex items-center gap-2 px-8 py-4 border-2 border-primary text-primary 
                     hover:text-white rounded-lg transition-all duration-300 relative overflow-hidden group"
              onclick="document.getElementById('contact').scrollIntoView({ behavior: 'smooth' })"
            >
              <span class="relative z-10">Get in Touch</span>
              <svg 
                class="w-5 h-5 relative z-10 group-hover:translate-x-1 transition-transform" 
                fill="none" 
                viewBox="0 0 24 24" 
                stroke="currentColor"
              >
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                      d="M20 12h-9m3 3l-3-3 3-3" />
              </svg>
              <div class="absolute inset-0 bg-primary translate-y-[200%] group-hover:translate-y-0 
                         transition-transform duration-500"></div>
            </a>
          </div>

          {/* Tech Stack */}
          <div class="pt-12">
            <h3 class="text-white/50 font-mono text-sm uppercase tracking-wider flex items-center gap-2 mb-6">
              <span class="w-8 h-[1px] bg-primary/40"></span>
              Tech Expertise
            </h3>
            <div class="flex flex-wrap gap-3">
              {[
                'Full Stack Development', 'NLP Research', 'Project Management',
                'Problem Solving', 'System Architecture', 'Cloud Solutions'
              ].map((skill) => (
                <div class="px-4 py-2 bg-dark-200/50 backdrop-blur-sm rounded-full border border-primary/20 
                            hover:border-primary/40 hover:bg-dark-200/70 transition-all duration-300 group">
                  <span class="text-white/70 text-sm group-hover:text-white/90 transition-colors">{skill}</span>
                </div>
              ))}
            </div>
          </div>
        </div>

        {/* Right Column - Code Editor */}
        <div class="relative hidden lg:block">
          <div class="relative backdrop-blur-xl rounded-xl overflow-hidden border border-primary/10 
                      shadow-2xl shadow-primary/5 transform hover:-translate-y-1 transition-transform duration-300">
            {/* Editor Header */}
            <div class="bg-dark-200/90 p-4 border-b border-primary/10">
              <div class="flex items-center">
                <div class="flex gap-2">
                  <div class="w-3 h-3 rounded-full bg-red-500/30"></div>
                  <div class="w-3 h-3 rounded-full bg-yellow-500/30"></div>
                  <div class="w-3 h-3 rounded-full bg-green-500/30"></div>
                </div>
                <div class="flex-1 flex justify-center">
                  <span class="text-white/50 text-sm font-mono px-3 py-1 rounded-md bg-dark-300/50">
                    philosophy.ts
                  </span>
                </div>
              </div>
            </div>

            {/* Editor Content */}
            <div class="p-8 space-y-8 bg-dark-200/50">
              {/* Against Section */}
              <div class="space-y-4">
                <div class="flex items-center gap-3">
                  <span class="text-purple-400/90 font-mono">// What I am againt is The "Hustle Culture" Trap</span>
                  <span class="px-2 py-0.5 text-xs font-mono bg-red-500/10 text-red-400/90 rounded-full">
                    @deprecated
                  </span>
                </div>
                <div class="pl-6 space-y-2">
                  <div class="relative">
                    <div class="absolute -left-4 top-0 bottom-0 w-[2px] bg-red-400/20"></div>
                    <div class="space-y-2 text-red-400/70 font-mono leading-relaxed line-through opacity-70">
                      <p>Sleep for 28 dayss,</p>
                      <p>grind for 48 hours,</p>
                      <p>cry about burnout.</p>
                      <p>Repeat.</p>
                    </div>
                  </div>
                </div>
              </div>

              {/* Beliefs Section */}
              <div class="space-y-4">
                <div class="flex items-center gap-3">
                  <span class="text-purple-400/90 font-mono">// What I believe in is Sustainable Development Philosophy</span>
                  <span class="px-2 py-0.5 text-xs font-mono bg-green-500/10 text-green-400/90 rounded-full">
                    @current
                  </span>
                </div>
                <div class="pl-6 relative">
                  <div class="absolute -left-4 top-0 bottom-0 w-[2px] bg-green-400/20"></div>
                  <div class="space-y-2 text-green-400/90 font-mono leading-relaxed">
                    <p>Consistent daily progress</p>
                    <p>15 minutes daily > 48 hours of burnout</p>
                    <p>Sustainable development</p>
                    <p>Work-life harmony</p>
                  </div>
                </div>
              </div>

              {/* Philosophy Box */}
              <div class="mt-8">
                <div class="p-4 bg-dark-300/50 rounded-lg border border-primary/10 
                            backdrop-blur-sm hover:border-primary/20 transition-colors">
                  <div class="space-y-3">
                    <p class="text-white/70 font-mono text-sm leading-relaxed">
                      <span class="text-primary/80">const</span> philosophy <span class="text-white/50">=</span> {
                      <span class="text-green-400 block pl-4">
                        "Small steps daily > Giant leaps at deadline"
                      </span>
                      };
                    </p>
                    <div class="pt-2 border-t border-primary/5">
                      <p class="text-white/60 font-mono text-sm italic flex items-center gap-2">
                        <span class="w-2 h-2 rounded-full bg-green-500/50 animate-pulse"></span>
                        Want to discuss sustainable development practices? Let's grab coffee ☕
                      </p>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>

          {/* Floating Elements */}
          <div class="absolute -top-8 -right-8 w-40 h-40 bg-primary/10 rounded-full blur-3xl"></div>
          <div class="absolute -bottom-8 -left-8 w-40 h-40 bg-accent/10 rounded-full blur-3xl"></div>
        </div>
      </div>
    </div>
  </div>

  {/* Scroll Indicator */}
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce">
    <svg class="w-6 h-6 text-white/50" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
    </svg>
  </div>
</section>

<style>
  .bg-grid-pattern {
    background-image: linear-gradient(rgba(255, 255, 255, 0.1) 1px, transparent 1px),
                      linear-gradient(90deg, rgba(255, 255, 255, 0.1) 1px, transparent 1px);
    background-size: 20px 20px;
  }

  @keyframes waveform {
    0%, 100% {
      transform: scaleY(0.5);
    }
    50% {
      transform: scaleY(1);
    }
  }
</style>